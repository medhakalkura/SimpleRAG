# Copy this file to .env and fill in values. Do not commit .env.

# Required for exercises/rag_chain.py (Claude)
ANTHROPIC_API_KEY=

# Required for exercises/image_gen.py when generating images
NVIDIA_API_KEY=

# Optional: only if you use OpenAI-based embeddings instead of local models
# OPENAI_API_KEY=
